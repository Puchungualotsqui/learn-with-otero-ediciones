package assignmentContentProfessor

import (
)

templ AssignmentContentProfessor(panels ...templ.Component) {
	<div class="flex flex-col lg:flex-row flex-1 h-[calc(100vh-5rem)]">
		for _, panel := range panels {
			@panel
		}
</div>

}
